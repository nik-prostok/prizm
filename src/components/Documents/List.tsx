import * as React from "react";
import {Typography} from "@material-ui/core";

export const DocumentsList = () => {

    return (
        <Typography variant="body1" gutterBottom>
            тут таблица с доками
        </Typography>
    )
}
